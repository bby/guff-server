<table border="2">
<%
@locs.each do |l|%>
<tr>
    <td><%= l.token_id %></td>
    <td><%= l.latitude %></td>
    <td><%= l.longitude %></td>
    <td><%= l.updated_at %></td>	
</tr>
<%  
end
%>
</table>